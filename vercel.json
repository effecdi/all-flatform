{
  "version": 2,
  "buildCommand": "npx vite build && npx esbuild server/vercel-entry.ts --bundle --platform=node --target=node18 --format=cjs --outfile=api/handler.js --external:pg-native --external:lightningcss --external:fsevents --alias:@shared=./shared",
  "outputDirectory": "dist/public",
  "framework": null,
  "functions": {
    "api/handler.js": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api/handler"
    },
    {
      "source": "/:path*",
      "destination": "/index.html"
    }
  ]
}
